# Arogya Disha — Community Engagement Project
Django>=5.0,<6.0

# Machine learning
scikit-learn>=1.4
pandas>=2.1
numpy>=1.26
joblib>=1.3

# Lab report reading (optional — the app falls back to manual entry without it)
# Requires the Tesseract binary as a SYSTEM package, not just this wrapper:
#     Ubuntu/Debian :  sudo apt install tesseract-ocr
#     macOS         :  brew install tesseract
#     Windows       :  https://github.com/UB-Mannheim/tesseract/wiki
pytesseract>=0.3.10
opencv-python-headless>=4.9
Pillow>=10.2
